# .understandignore — patterns for files/dirs to exclude from analysis
# Syntax: same as .gitignore (globs, # comments, ! negation, trailing / for dirs)
#
# Built-in defaults (always excluded unless negated):
#   node_modules/, .git/, dist/, build/, obj/, *.lock, *.min.js, etc.
#

# --- Dependencies / build output / generated ---
/node_modules
/.next/
/out/
/build
/src/generated
package-lock.json
*.tsbuildinfo
next-env.d.ts

# --- Tooling / editor / secrets ---
.claude/
.vercel
.DS_Store
*.pem
.env
.env.example
.env.*

# --- Legacy prototype (superseded by the Next.js app; migrated into DB) ---
추가 작업물/

# --- Debug logs ---
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
